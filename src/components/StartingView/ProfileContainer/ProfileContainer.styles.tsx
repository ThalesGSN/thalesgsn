import styled from 'styled-components';


export const ProfileContainerArticle = styled.article`
  grid-area: Profile;
  place-self: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  font-size: clamp(14px, 10vw, 72px);
  line-height: 0;
`;